services:
  # Database (Supabase Compatible)
  postgres:
    image: postgres:15-alpine
    container_name: ctf_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ctf_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - ctf_backend
      - ctf_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend API
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: ctf_api
    environment:
      - API_HOST=0.0.0.0
      - API_PORT=8000
      - SECRET_KEY=${SECRET_KEY:-super-secret-key}
      # Supabase Config
      - SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL}
      - SUPABASE_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY}
      - SUPABASE_ANON_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY}
      - SUPABASE_SERVICE_KEY=${SUPABASE_SERVICE_KEY}
      # Container Host (for generated container URLs)
      - CONTAINER_HOST=${CONTAINER_HOST}
      # Container Resource Limits (Ver 10.2)
      - CONTAINER_CPU_LIMIT=${CONTAINER_CPU_LIMIT:-0.5}
      - CONTAINER_MEMORY_LIMIT=${CONTAINER_MEMORY_LIMIT:-128m}
      - CONTAINER_PIDS_LIMIT=${CONTAINER_PIDS_LIMIT:-50}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./api:/app
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - ctf_backend
      - ctf_net
    restart: unless-stopped
    # Resource limits for API service itself (cost control)
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512m
        reservations:
          cpus: '0.5'
          memory: 256m

  # Frontend (Next.js)
  frontend:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: ctf_frontend
    environment:
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
      - NEXT_PUBLIC_SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL}
      - NEXT_PUBLIC_SUPABASE_ANON_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY}
      - NODE_ENV=development
    ports:
      - "3000:3000"
    volumes:
      - ./web:/app
      - /app/node_modules
    depends_on:
      - api
    networks:
      - ctf_backend
      - ctf_net
    restart: unless-stopped

networks:
  ctf_backend:
    driver: bridge
  ctf_net:
    external: true

volumes:
  postgres_data: